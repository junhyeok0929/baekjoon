#include<iostream>
using namespace std;

int main() {
	int x, y;
	cin >> x; //x 입력
	cin >> y; //y 입력
	if (y < 45)
		if (x == 0)
			x = 23;
		else
			x = x - 1;
	if (y >= 45)
		y = y - 45;
	else          
		y = y + 15;
	cout << x << " " << y;
		
	return 0;
}
